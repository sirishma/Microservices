server:
  port: 1995
  
spring:
  cloud:
    config:
      server:
        git:
          uri: 
        native:
          search-locations: file://${user.home}/config/
           
  application:
    name: CONFIG SERVER
    
eureka:
  client:
    register-with-eureka: true
#SPRING_PROFILES_ACTIVE: native         
          
